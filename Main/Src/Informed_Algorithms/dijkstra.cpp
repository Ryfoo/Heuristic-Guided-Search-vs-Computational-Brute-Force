#include "../include/dijkstra.hpp"

const std::string Dijkstra::NAME = "Dijkstra";

SearchResult Dijkstra::solve(const Maze& maze) {
    // TODO
}

std::vector<Cell> Dijkstra::reconstruct_path(
    const std::vector<std::vector<Cell>>& parent,
    Cell start,
    Cell goal)
{
    // TODO
}
